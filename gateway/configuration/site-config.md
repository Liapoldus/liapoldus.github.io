# Frontend roots

Frontend files публикуются и откатываются вместе с Caddyfile в составе
[group revision](/gateway/api/groups). Это страница-указатель для старого URL;
нормативная модель находится в API groups.
