# Справка по `gateway.yaml`

Актуальны только bootstrap-поля из [канонического описания](bootstrap) и
[JSON Schema](/spec/gateway.schema.json). Трафик настраивается Caddyfile в
[group releases](/gateway/api/groups); отдельного Gateway YAML-языка маршрутов
нет.
