# Старая корневая схема

Прежняя Gateway YAML-схема удалена: в целевой v1 `gateway.yaml` содержит
только bootstrap-параметры. Актуальное описание и схема находятся в разделе
[Bootstrap-конфигурация](/gateway/configuration/bootstrap).
